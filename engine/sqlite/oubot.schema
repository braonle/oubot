CREATE TABLE IF NOT EXISTS tggroupparams(
    id INTEGER PRIMARY KEY,
    chat_id INTEGER NOT NULL UNIQUE,
    hour_fee INTEGER NOT NULL DEFAULT 1200
);

CREATE TABLE IF NOT EXISTS tggroupbalance(
    id INTEGER PRIMARY KEY,
    chat_id INTEGER NOT NULL UNIQUE,
    balance INTEGER DEFAULT 0
);